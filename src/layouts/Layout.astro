---
import { ClientRouter } from 'astro:transitions';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Musicaneta',
  description = 'Reproductor de Audio Minimalista y Futurista',
} = Astro.props;
---

<!doctype html>
<html lang="es" class="dark bg-[#0e0a1f] text-purple-50 antialiased h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="192x192" href="/favicon-192x192.png" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" href="/favicon-192x192.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <ClientRouter />
  </head>
  <body class="min-h-[100dvh] bg-[#0e0a1f] text-purple-50 selection:bg-purple-500 selection:text-white overflow-x-hidden">
    <main class="min-h-[100dvh] w-full">
      <slot />
    </main>
  </body>
</html>
